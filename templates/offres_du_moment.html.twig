{% extends 'base.html.twig' %}

{% block title %}HalalSweetShop
{% endblock %}

{% block body %}

<div class="cards">
{% for produit in offresDuMoment %}

    <div class="card-custom">
        <img src="{{ asset('img/offers/' ~ produit.image) }}" class="card-img-top" alt="{{ produit.title }}">
        <div class="card-body">
            <h5 class="card-title">{{ produit.title }}</h5>
            <p class="card-text">Prix : {{ produit.price }} €</p>
        </div>
    </div>
    
{% endfor %}
</div>

{% endblock %}